{
  "name": "@repo/db",
  "version": "1.0.0",
  "description": "Shared database entities, schemas, and DTOs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "lint": "echo \"no lint setup yet\""
  },
  "dependencies": {
    "@nestjs/config": "^4.0.2",
    "bcrypt": "^6.0.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.2",
    "crypto": "^1.0.1",
    "nanoid": "^5.1.6",
    "reflect-metadata": "^0.2.2",
    "typeorm": "^0.3.27",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "typescript": "^5.9.3"
  }
}
